import Loading from "@/components/loading/loading";
import React from "react";

const page = () => {
  return (
    <div className="w-fit h-screen">
      <Loading />
    </div>
  );
};

export default page;
